{
  "name": "auditoria-blockchain",
  "version": "1.0.0",
  "description": "Projeto de auditoria baseado em blockchain para rastreabilidade e transparência de dados de doações para ONGs.",
  "private": false,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest",
    "pnpm": "latest"
  },
  "engines": {
    "pnpm": ">=10.0.0",
    "node": ">=22.0.0"
  }
}
